using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CustomDragScrollView : UIDragScrollView {

	public void childDrag(Vector2 delta){
//		Debug.Log ("childDrag called" + delta.x + " and " + delta.y);
//		scrollView.Press(true);
//		scrollView.MoveRelative(new Vector3(delta.x,delta.y,0f));
//		scrollView.Drag();
//		base.OnScroll (delta.x);
//		scrollView.Drag ();
	}
}
